<div class="all-items">
<h1>All the menus</h1>

<% @menus.reload.each do |menu| %>
  <div class="item-card <%= menu.activated == 1 ? 'activated' : '' %>">
      <a href="/menus/<%= menu.slug %>"><%= menu.name %></a>
      <% menu.meals.each do |meal| %>
      <p><%= meal.name %></p>
      <% end %>
    </form>
  </div>
<% end %>
</div>
