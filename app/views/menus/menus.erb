<div class="all-items">
<h1>All the menus</h1>

<% @menus.each do |menu| %>
  <div class="item-card">
    <a href="/menus/<%= menu.slug %>"><%= menu.name %></a>
    <% menu.meals.each do |meal| %>
    <p><%= meal.name %></p>
    <% end %>
  </div>
<% end %>
</div>
